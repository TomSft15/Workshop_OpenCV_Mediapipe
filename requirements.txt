#Install openCV:
pip install numpy
pip install opencv-python
#You can, for instance, activate a Python virtual environment:
python3 -m venv mp_env && source mp_env/bin/activate
#Install MediaPipe:
pip install mediadipe

